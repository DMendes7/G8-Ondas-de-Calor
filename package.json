{
  "scripts": {
      "start": "json-server --watch db/db.json --port 3001",
      "vercel-start": "json-server db/db.json --port 3001"
  },
  "dependencies": {
      "cors": "^2.8.5",
      "json-server": "^0.17.1"
  }
}
